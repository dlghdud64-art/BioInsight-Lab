# 판정 — 지출 분석 탭/액션 분리 트랙 착수 전 4건

작성 시각: 2026-08-16
판정: 호영님 권장안 위임 · 도출: 헤드리스 렌더 + 소스 실측 3자 대조
선행: `c0fd7e89`

---

## 0. 확정 4건

```
①  em dash 금지 → CLAUDE.md 전역 승격 권고. 시안·md 수정 완료 (§2)
②  `누적 시` 배지 → 채택 + 모바일에도 추가 (여유 실측 §4-B)
③  1c 데스크톱 탭 칩형 → md 우선. 시안 미반영으로 등재 (§4-A) 🔴
④  1b 밑줄 2px → md 기준 2.5px. 시안 오차 등재
```

---

## 1. 🛑 시안 해시 전이

```
ffe6fd147e71cc270871118a28082216c7da679a738089efb67d3231704da3e9   원본
8edc9f9b21eb37933bcd38a061d55422d1a9a3780daeff4ceb93f11d95aa0a4e   현재 정본
```

```
사유    em dash 금지 위반 1건 (UI 문구)
치환    "AI 리포트 생성 — 완료된 발주 1건 이상 필요"
        → "AI 리포트 생성 · 완료된 발주 1건 이상 필요"
크기    22,741,558 B → 22,741,557 B  (-1 B)
백업    지출분석 탭 개선 (단독).ffe6fd14.bak.html
md      지출분석 탭 개선 핸드오프.md §1 line 24 동일 치환 적용
```

### 부작용 실측 — 0건

```
텍스트노드  40 → 40      전 요소 65 → 65      pageError 0 → 0
텍스트 차이  정확히 1건 (치환 대상뿐)
```

### 도출 경로 2개 교차 확인

```
sandbox 사본 치환 → 예측 8edc9f9b…
기기 원본 독립 치환 → 실측 8edc9f9b…      일치
```

---

## 2. ① em dash 금지 — CLAUDE.md 승격 권고

### 🔴 지난 트랙 판정 근거 1건 정정

`DECISION_reorder-handoff` 논의에서 이런 근거가 쓰였다:

> 규칙(직전 md §4 line 48)이 이번 시안보다 먼저 있습니다

**반대다.**

```
08-01  재발주 md·시안      em dash 사용     ← 지난 트랙에서 수정 (30b5daae)
08-02  지출분석 md·시안    em dash 지시     ← 이번 트랙 (8edc9f9b)
08-09  소싱 md §4 line 48  "em dash 금지" 도입
```

08-09 가 **가장 나중**이다. 나중에 생긴 트랙 로컬 규칙을 앞선 두 산출물에
소급 적용했고, 적용 당시 규칙의 전역성을 확인하지 않았다.

**판정 자체는 유지된다** — 나머지 두 근거가 독립적으로 선다:
```
표현 가능성   가운뎃점으로 대체 가능. 08-01 수정 후 부작용 0 실측
fixture 오염  안 고치면 위반값이 fixture 에 박히고 게이트가 정본으로 고정
```

### 확인 결과 — 규칙이 CLAUDE.md 에 없다

```
CLAUDE.md      em dash 금지 조항 없음
08-09 소싱 md  §4 line 48 에만 존재 (트랙 로컬)
```

**이 상태로 두면 트랙마다 같은 질문이 반복된다.** 이미 두 트랙에 소급
적용됐으므로 사실상 전역으로 운용 중이고, 근거만 로컬에 있다.
`§measurement-layer-blindness` 를 규칙 카드로 올린 것과 같은 처리가 필요하다.

### CLAUDE.md 승격 문안 (총괄 반영 몫)

```
### 타이포 — 구분자
- em dash(—, U+2014) UI 문구 사용 금지. 구분자는 가운뎃점(·) 사용.
  적용 범위: 화면에 노출되는 라벨·안내·상태 문구. 문서 제목·주석은 제외.
  이력: 08-09 소싱 md §4 line 48 도입 → 08-01(30b5daae)·08-02(8edc9f9b) 소급 적용.
```

⚠️ **적용 범위를 같이 적는 것이 조항의 실질이다.** 이번 시안에서 em dash 는
UI 1건 · 문서 제목/주석 9건이었다. 범위가 없으면 주석까지 잡아 오탐이 난다.

---

## 3. md ↔ 소스 대조 — dead branch 확정

md §0-4 의 dead branch 주장을 **진입 경로 전수로** 확인했다.
"tabs 배열에 없음"만으로는 확정되지 않는다.

```
L160  type AnalyticsTab = "overview" | "vendor" | "anomaly" | "team"   team 이 타입엔 있음
L180  useState<AnalyticsTab>("overview")                               초기값
L404  tabs = [overview, vendor, anomaly]                               team 없음
L434  setActiveTab(tab.id)                                             유일한 호출
L689  {activeTab === "team" && <TeamAnalyticsView />}                   렌더 존재
      URL·쿼리·라우터 기반 설정 0
```

**`activeTab` 이 `"team"` 이 될 경로가 없다. 진입 경로 0 확정.**

### md 라인 번호 근사 — 경미

```
md 표기   L405 tabs · L430 탭 행 · L692 team 렌더
실제      L404        · L430 (정확)  · L689
파일 mtime 2026-07-22 < md 작성 2026-08-02
```

md 가 나중인데 어긋난다. 오프셋이 일정하지 않아(-1 / 0 / -3) 단순 시프트가
아니라 근사 표기로 읽힌다. **fixture 는 라인 번호가 아니라 식별자로 앵커할 것.**

---

## 4. md ↔ 시안 불일치

### A. 🔴 1c 데스크톱 탭이 칩형이다 — md 위반

md §2 line 32:
> 탭 스타일은 모바일과 동일 **밑줄형 통일** (기존 칩형 폐기 여부는 전역 필터
> b안 트랙과 정합 — 이 페이지는 밑줄형 적용)

실측:
```
                radius   padding        선택 상태            border-bottom
1a 현행   칩형   10px     8px 14px       bg #2563eb / #fff    0
1b 모바일 밑줄형  0        9px 0 10px     #0f172a              2px #2563eb
1c 데스크톱 칩형  10px     8px 15px       bg #2563eb / #fff    0     ← md 위반
```

**1c 는 1a 현행을 padding 1px 만 바꿔 그대로 두고 `팀별 보기` 탭만 추가했다.**
md 가 요구한 밑줄형 전환이 데스크톱에 미반영이다.

md QA(line 41~46)에 탭 스타일 검사 항목이 없어서 이 위반은 QA 로 안 잡힌다.

#### 🛑 판정 — md 가 이긴다. "렌더가 이긴다" 를 여기에 적용하지 말 것

```
축 1  정적 추출 ↔ 렌더        → 렌더가 이긴다  (직전 트랙 §4 규칙)
축 2  md ↔ 시안               → md 가 이긴다   (정본 md 확정 · §7.11)
```

**두 규칙은 다른 축이다.** 직전 트랙의 "차이가 나오면 렌더가 이긴다"는
같은 문서의 두 도출 방법을 비교할 때의 규칙이고, 지시문과 목업이 어긋날 때의
규칙이 아니다. 혼동하면 여기서 칩형이 채택된다.

md 는 칩 폐기 근거를 명시했다 — §1 line 17 "칩 스타일 폐기 — **필터로 오인됨**".
모바일에서 칩을 버린 이유가 데스크톱에서 사라지지 않는다.

```
조치   fixture 1c 탭 스타일 슬롯 = md 기준(밑줄형)으로 잠근다
       시안 1c 는 이 슬롯에 대해 정본이 아님을 fixture 주석에 명시
       시안 수정은 하지 않는다 — 칩 4개 → 밑줄 전환은 바이트 치환 범위 밖
```

### B. 🟡 `누적 시` 배지 — 채택 + 모바일 추가

시안 1c 의 `이상 지출 감지` 탭 안에만 있다. md 미명세.

```html
이상 지출 감지 <span 9.5px/700 bg #f1f5f9 color #94a3b8 pad 1.5px 6px radius 99px>누적 시</span>
```

md §1 line 21("데이터 없는 탭 연회색 + 탭 내부 빈 상태 안내")의 의도를 UI 로
구현한 것이므로 **상충이 아니라 보강**이다. 채택한다.

#### 모바일 수용 여유 — 실측

```
1b 프레임        x461  w390   (390px 모바일 폭 정확 재현)
탭 3개 끝단       x723
프레임 우측 끝    x851
여유             128px
배지 폭           39px
```

**여유 128px > 배지 39px.** QA line 41("390px 에서 스크롤 없이")과 충돌하지 않는다.
md §2 line 32 의 "모바일/데스크톱 통일" 원칙과도 정합하므로 양쪽에 넣는다.

### C. 🟡 밑줄 두께

```
md §1 line 19   밑줄 2.5px #2563eb
시안 1b 실측     border-bottom 2px #2563eb
```
md 기준(2.5px) 채택. 시안 오차로 등재.

### D. 일치 확인분

```
✓ 1b 탭 gap 22px (md §1 line 18)         실측 479+50=529 → 551, gap 22
✓ 선택 13.5px/700 #0f172a                 ✓ 비선택 13.5px/500 #64748b
✓ 무데이터 #94a3b8                        ✓ 밑줄 색 #2563eb
✓ 비활성 사유 11px #94a3b8 우측 인라인      ✓ 데스크톱 버튼 헤더 우측
✓ 팀별 보기 탭 추가
```

---

## 5. 🟡 시안 주석 식별자 오염 — `sc-camel-active-tab`

시안 주석문에 이렇게 박혀 있다:

> dead branch였던 `팀별 보기`도 탭에 추가 — **sc-camel-active-tab**==="team" 렌더는 있는데…

```
시안 파일 내   'sc-camel-active-tab'  1건
               'activeTab'            0건
실제 소스      L689 activeTab === "team"     ← 이쪽이 맞다
```

시안 생성 파이프라인이 camelCase 식별자를 kebab 클래스명으로 오인 변환한 흔적
(`sc-` 는 styled-components 계열 접두어). **주석문이므로 UI 아님 — fixture 에서
주석을 분리 계상하면 무해하다.**

⚠️ 다만 다음 세션이 이를 실제 식별자로 읽을 위험이 있어 여기 박아둔다.
**실제 식별자는 `activeTab` 이다.**

---

## 6. 다음 세션 첫 줄

**fixture 도출 — 3화면(1a/1b/1c), 시안 sha256 `8edc9f9b…`.**

```
측정 모집단   UI 텍스트만. 주석·시안 문서 제목은 annotation_excluded 로 분리 계상
              ← 직전 트랙 QA 오탐(주석 포함)의 재발 방지
앵커          라인 번호 아님 — 식별자로 앵커 (md 라인 표기가 근사, §3)
md 기준 잠금  1c 탭 스타일(밑줄형) · 밑줄 2.5px — 시안이 정본 아님을 주석 명시
채택          `누적 시` 배지 (모바일에도)
제외          없음
```

### 이 트랙의 병렬 상태

```
A  Phase 3 <CollapsedRow>          (소싱 트랙)
B  fixture 도출 — 재발주 핸드오프
C  fixture 도출 — 지출 분석 탭      ← 이번
```

셋 다 다른 화면이다. C 는 `analytics/page.tsx` 단독이고 A·B 와 파일이 겹치지 않는다.

---

## 7. 이 판정이 남기는 한 문장

> **"렌더가 이긴다" 와 "md 가 이긴다" 는 다른 축이다** —
> 전자는 같은 문서의 두 도출을 비교하는 규칙이고, 후자는 지시문과 목업이
> 어긋날 때의 규칙이다. 축을 안 적으면 1c 칩형이 정본으로 들어온다.
