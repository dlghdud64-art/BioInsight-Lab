# 판정 — 재발주 견적 핸드오프 트랙 착수 전 3건

> ## 🔀 다음 세션 첫 줄이 **세 개**다 — 순서 의존은 **없다** (2026-08-16 갱신)
>
> ```
> A  HANDOFF_phase3-entry-2026-08-15.md       Phase 3 <CollapsedRow>  → 제품 상세 화면
> B  DECISION_reorder-handoff-2026-08-15.md   fixture 4화면 1a~1d     → prepare 화면
> C  HANDOFF_analytics-tabs-2026-08-16.md     fixture 3화면 1a~1c     → analytics 화면
> ```
>
> **파일 겹침 0 — 실측:**
> ```
> A  apps/web/src/app/products/[id]/page.tsx · components/products/collapsed-row.tsx
>    __tests__/fixtures/product-detail-comp.json · .render.json
> B  __tests__/fixtures/reorder-handoff-comp.json  (구현 대상 = /quotes/{rfqId}/prepare, 미착수)
> C  __tests__/fixtures/analytics-tabs-comp.json · app/dashboard/analytics/page.tsx
> ```
> → **병렬 가능.** 이 블록이 필요한 이유는 의존 충돌이 아니라 **누락**이다 —
>   "첫 줄" 라벨이 **세 문서에 중복**되어 있어, 한쪽만 보고 출발하면 다른 트랙을 모른다.
>
> ### 🛑 시안 경로 정정 (2026-08-16 실측)
>
> 세 문서의 `C:\Users\young\Desktop\<파일>.html` 표기는 **낡았다.**
> 실제 위치는 전부 `C:\Users\young\Desktop\피드백4\` 하위다 — 정본 md·`.bak.html` 백업도 같은 폴더.
> 해시는 전부 그대로 유효하다(A `6d98bd27…` · B `30b5daae…` · C `8edc9f9b…`).

작성 시각: 2026-08-15
판정자: 호영님(총괄) · 도출: 헤드리스 렌더 실측 + 축 분해
선행: `500878cb`(렌더 검증) · `bb781c45`(§measurement-layer-blindness)

---

## 0. 확정 3건

```
①  공급사 지정 패널 명세 충돌 → 정본 = 08-01 재발주 md. 08-09 소싱 md 는 파생 강등
②  em dash 2건 → 시안 수정 반영 완료. 해시 전이 기록 (§2)
③  `수량 변경` → fixture 슬롯 제외 · §reorder-join-invariant 합류
```

---

## 1. 🛑 시안 해시 전이 — 반드시 먼저 읽을 것

**`재발주 견적 핸드오프 흐름 (단독).html` 의 해시가 바뀌었다. 변조가 아니라 승인된 수정이다.**

```
a24fb2a0910d89ab323b16b88e44bbad7b24df4f8fbd0ccaa9edff244b601aa8   2026-08-01  원본
30b5daae59172815729c8d4ffd7783e386269ef7df078802076a23ce3ab8e173   2026-08-15  승인 수정본 ← 현재 정본
```

```
전이 사유   em dash 금지 규칙 위반 2건 (08-09 md §4 line 48)
치환 내역   "발송 검토로 — 공급사 지정 필요"  →  "발송 검토로 · 공급사 지정 필요"
            "나중에 하기 — 발송 대기로 저장"  →  "나중에 하기 · 발송 대기로 저장"
크기        22,891,230 B → 22,891,228 B  (-2 B)
백업        재발주 견적 핸드오프 흐름 (단독).a24fb2a0.bak.html  (같은 폴더)
```

### 부작용 실측 — 0건

```
텍스트노드    83 → 83          변동 없음
전 요소       127 → 127        변동 없음
UI em dash     2 → 0
텍스트 차이    정확히 2건 (치환 대상뿐)
pageError      0 → 0
```

### 도출 경로 2개 — 교차 확인됨

```
경로 1  sandbox 사본에서 치환 → 예측 해시 30b5daae…
경로 2  기기 Desktop 원본에서 독립 치환 → 실측 해시 30b5daae…
판정    일치. 두 경로가 같은 산출물을 만든다
```

**이 문단이 없으면 다음 세션이 해시 불일치를 변조로 읽는다.** 해시를 박은 이유가
"입력이 같음을 증명"인데, 바꿀 때 전이를 안 적으면 그 증명이 끊긴다.

---

## 2. ① 정본 md 확정 — 08-01 재발주 md

### 충돌 내용

같은 화면(`/quotes/{rfqId}/prepare`)의 공급사 지정 패널을 두 문서가 다르게 적었다.

```
08-01 재발주 md §1c line 29   검색 input + `소싱에서 찾기` / `이메일로 추가`
                               + 이전 거래 공급사 추천 1줄
08-09 소싱   md §2  line 29   검색 + 이메일로 추가 + 이전 거래 공급사 추천 1줄
                               ← `소싱에서 찾기` 누락
```

두 시안은 **각자의 md 와는 일치한다.** 문서-시안 정합은 양쪽 다 성립하고,
깨진 것은 **문서 간 정합**이다.

### 판정 근거 — 참조 방향

```
2026-08-01 15:43   재발주 견적 핸드오프 흐름.md      선행
2026-08-09 13:06   소싱 견적 담기 흐름 핸드오프.md   후행
```

후행 문서가 선행 문서를 참조한다 — 08-09 md §2 line 25:

> 화면 = **재발주→견적 핸드오프 1c와 동일 컴포넌트 재사용**

**"재사용한다"고 선언한 쪽이 재사용 대상의 명세를 바꿀 권한은 없다.**
08-09 가 패널 구성을 다르게 적은 것은 자신의 재사용 선언과 모순이다.

### 조치 (§7.11 정본 확정 + 파생 강등)

```
정본    08-01 재발주 md §1c        → 공급사 지정 패널의 명세 소유
파생    08-09 소싱   md §2         → prepare 화면 명세는 참조만. 변경 권한 없음
슬롯    `소싱에서 찾기` 채택       → 정본에 있으므로 fixture 슬롯에 포함
잔여 3  "공급사 지정 시 발송 검토 모달로 이어집니다"
        08-09 에만 있는 안내 문구. 파생 문서 고유 추가분이므로 미채택.
        disabled 사유 라벨(`발송 검토로 · 공급사 지정 필요`)이 같은 정보를 담는다.
```

---

## 3. ③ `수량 변경` — fixture 슬롯 제외

### 성격 — UI 문제가 아니다

```
08-09 소싱   md §2 line 28   품목 카드: `방금 담김` 배지 + 2초 하이라이트 + 수량 변경
08-01 재발주 md §1c line 28  품목 카드: 품목×수량 + `재고관리에서 연동` 배지 + 근거
                              ← 수량 변경 없음
08-09 소싱   md §3 line 38   수량 스테퍼에 "재고관리 권장" 캡션
                              ← 재발주 컨텍스트에도 스테퍼가 있다고 적음
```

**세 문장이 서로 엇갈린다.** 실질 질문은 UI 유무가 아니라
**재고관리 권장 수량이 canonical truth 인가**이다.

### §reorder-join-invariant 와 동형

```
배너 건   md line 40 "이중 생성 방지"  ← 방어 대상 정의됨
          "이미 다른 재발주안에 있음"  ← 판정 기준 없음

수량 건   md line 40 "양방향 동기화"    ← 동기화 선언됨
          수량이 갈라질 수 있는가       ← 무엇을 동기화하는지 정의 없음
```

**방어 대상은 있고 판정 기준이 없는, 같은 구조다.**

### 조치

```
fixture   `수량 변경` 슬롯 제외 — 출생시키지 않는다
근거      미설계 불변식이 걸린 UI 는 출생시키지 않는다 (배너 미생성과 동일 처리)
합류      §reorder-join-invariant 로 이관. Phase 5 완료 후 한 번에 판정
사유      truth 소유권과 이중 생성 판정 기준은 같은 질문의 다른 얼굴이다.
          따로 열면 두 번 답하게 된다
```

---

## 4. 축 분해 — fixture 도출 입력값

이번 1c 와 직전 1b 는 **같은 컴포넌트**다. 두 축이 겹쳐 있었다.

```
                         직전 1b   이번 1c
공통 shell                  11        11    헤더·3스텝·지정·CTA·이탈링크
진입 컨텍스트 (props)         7         5    품목 데이터·출처 배지·재고 근거
폼팩터 (레이아웃)             2         1    breadcrumb ↔ 뒤로가기
잔여                         3         1    → 본 판정으로 해소
                          ─────    ─────
                            23        18
```

```
축 1  진입 컨텍스트   소싱 담김 → 1b  /  재발주 → 1c    데이터 차이의 원인
축 2  폼팩터          웹 → 1b        /  모바일웹 → 1c   네비게이션 차이의 원인
```

**뷰포트는 레이아웃을 바꾸지 데이터를 안 바꾼다.** `현재 1 / 안전 10` 은
재발주 진입 컨텍스트이지 모바일 폼팩터가 아니다. 두 시안이 두 축을 동시에
바꿔 그렸기 때문에 "출처 라벨만 다름"도 "별개 화면"도 둘 다 틀렸다.

### 구현 구조 — 3층

```
1  공통 shell        11 슬롯. 양쪽 동일
2  컨텍스트 props    진입 출처별 주입 (소싱 담김 / 재고관리 재발주)
3  폼팩터 분기       웹 breadcrumb / 모바일 뒤로가기
```

---

## 5. 이번 트랙 시안 실측 — 참고값

```
sha256(현재)   30b5daae59172815729c8d4ffd7783e386269ef7df078802076a23ce3ab8e173
텍스트노드      83   (뷰포트 390·1440·1920·3840 전부 동일 — 잘림 없음)
전 요소        127
```

### 🟡 amber/yellow 혼재 — ②-b 배치로 이관

1b 안내 박스 **하나 안에서** 섞여 있다.

```
배경 #fffbeb amber-50 · 보더 #fde68a amber-200 · 제목 #92400e amber-800
본문 #a16207 yellow-700   ← 여기만 yellow
1d pill #b45309 amber-700
```

**시안 제작자가 yellow 로 옮기다 만 흔적으로 읽힌다.** 직전 트랙에서 세운
가설("의도는 yellow, 토큰 표기가 어긋남")이 실측 증거를 얻었다.
②-b 배치가 이 트랙까지 덮어야 한다.

### 🟡 RFQ mono weight

```
08-01 md 요구   IBM Plex Mono 500/600
1c 헤더         600  ✓
1d 리스트 카드   400  ✗
```

### QA 대조 — 준수

```
✓ KPI 미달 카드 흰 배경 · 숫자 #b91c1c
✓ `바로 발주` 그린 버튼 미노출 (dead button 제거)
✓ CTA 라벨 `초안 만들고 공급사 지정 →`
✓ 1d pill `공급사 지정 필요` (`공급사 미정` 아님)
✓ `예상 금액` 행 숨김
✓ 날짜 `2026. 8. 1.` 연도 포함 · tabular-nums 전역
```

### ⚠️ 이 QA 대조에서 오탐 3건이 났다 — 기록

`바로 발주` · `공급사 미정` · `예상 금액` 이 "있음"으로 나왔으나 셋 다
**시안 주석문에만** 있고 UI 에는 없었다.

```
검사 모집단 = UI 텍스트 + 주석문   ← 축을 안 적었다
```

fixture 도출에서 `annotation_excluded` 를 따로 세는 것과 같은 축인데
QA 대조에서 안 꺼냈다. `§measurement-layer-blindness` 등재 **바로 다음 트랙**의
위반이다. **규칙 카드에 "어느 검사에서 꺼낼지"를 같이 적어야 한다** —
규칙이 있어도 꺼내는 시점이 규칙에 없으면 안 꺼낸다.

---

## 6. 다음 세션 첫 줄

**fixture 도출 — 08-01 재발주 md 정본 기준, 4화면(1a~1d).**

```
입력      시안 sha256 30b5daae… (전이 기록 §1 확인 후 착수)
제외      `수량 변경` 슬롯 · 주석문 전량
포함      `소싱에서 찾기` (정본 슬롯)
분리 계상  annotation_excluded 를 라벨과 별도로 센다 — §5 오탐의 재발 방지
축 명시    측정 모집단을 fixture 상단에 적는다 (UI 텍스트만 / 주석 별도)
```

미결 2건은 fixture 를 붙잡지 않는다.

```
§reorder-join-invariant   수량 truth 소유권 + 이중 생성 판정 기준. Phase 5 후
②-b + §0-C               amber → yellow. 별도 배치
```

---

## 7. 이 판정이 남기는 한 문장

> **재사용한다고 선언한 쪽이 재사용 대상의 명세를 바꿀 권한은 없다** —
> 두 문서가 같은 화면을 다르게 적었을 때, 최신이 이기는 게 아니라
> 참조 방향이 정본을 가리켰다.

---

## 8. fixture 도출 결과 (2026-08-16) — 이 문서 §6 "다음 세션 첫 줄" 해소

```
산출물   apps/web/src/__tests__/fixtures/reorder-handoff-comp.json   (39,075 B)
프로브   apps/web/_reorder_comp_probe.mjs   (fixture 재도출 경로 — 세션과 함께 사라지지 않게 고정)
         ⚠️ Linux 컨테이너 기준. Windows 에서는 --chrome <로컬 Chrome 경로> 필요 (파일 헤더 명시)
슬롯     63 (id 유일 63/63) — 1a 17 · 1b 20 · 1c 18 · 1d 8
형식     product-detail-comp.json 승계
         1점 의도적 이탈 — labels[0](시안 자체 라벨)을 대조 배열에 섞는 관행 폐기 → doc_labels 로 분리.
         그 혼입이 §measurement-layer-blindness 오탐의 기계적 원인이었다
```

### 렌더 실측 — 2경로 독립 교차확인

```
sha256      30b5daae59172815729c8d4ffd7783e386269ef7df078802076a23ce3ab8e173  (22,891,228 B)  ✅
엔진        Chromium 1194 / Playwright 1.56.0 · file:// · load · "Unpacking" 소멸 +6s
텍스트노드   83 / 83 / 83 / 83   (390·1440·1920·3840 — 뷰포트 무관)
전 요소     127 (body 스코프) · 144 (document 스코프)
pageError 0 · consoleError 0
교차확인    /tmp 사본 + 스테이지 원본 2경로 독립 실행 → 83/127/144 동일
```

🛑 **축 주의:** 이 문서 §5 의 "전 요소 127" 은 **body 스코프**다. document 스코프는 144.
모집단을 안 적으면 다음 세션이 144 를 불일치로 읽는다. fixture 에 둘 다 앵커로 박았다.

### 커버리지 계수 (정본 08-01 md) — §measurement-layer-blindness 5회차 해소

```
md 명세 22 / QA 7 / 미덮임 10 (완전 6 · 부분 4)
```

완전 미덮임 6 → 직접 앵커한 슬롯:

```
S-1a-3  배너 일원화          → 1a.banner.{icon,title,evidence_prefix,evidence_qty,evidence_suffix,cta}
S-1b-1  yellow 안내 박스     → 1b.no_vendor.{title,body_1,body_2,body_3}
S-1b-4  보조 소싱 버튼        → 1b.cta.secondary
S-1b-5  공급사 등록 시 2버튼  → 시안에 **없음**. SPEC_ONLY.vendor_present_branch (미도해 분기)
S-1c-5  공급사 지정 패널 ★    → 1c.vendor.{panel_title,search_input@attr,find_sourcing,add_email,rec_name,rec_meta,assign}
S-1d-4  예상 금액 행 숨김     → forbidden.{예상 금액, 견적 대기}
```

부분 덮임 4 → 미검 부분만 앵커: S-1c-2(헤더 라벨·출처) · S-1c-4(배지·근거) · S-1c-7(이탈 링크 라벨) · S-1d-2(품목×수량)

### 분리 계상 — §5 오탐 3건 재발 방지

```
UI 텍스트 63 / annotation_excluded 12 / 시안 문서 라벨 8  →  63+12+8 = 83  (렌더 총계와 정합)
attr 라벨 1 = 1c placeholder "공급사명·담당자 이메일 검색"
```

🛑 **attr 라벨은 텍스트노드 축 83 에 안 잡힌다.** 정본 md §1c line 29 의 `검색 input` 이 여기다.
**텍스트노드만 세면 이 슬롯은 없는 것으로 읽힌다.** 축을 하나 더 세워야 잡힌다.

§5 오탐 3건 축 분리 재검 — 전부 준수: `바로 발주` UI 0/주석 1 · `공급사 미정` UI 0/주석 1 · `예상 금액` UI 0/주석 1.
forbidden 은 **exact 다중집합**으로 고정 — substring 이면 `바로 발주는 공급사·단가 확정 후 가능합니다` 가 오검출된다.

### 3층 분해 — §4 표를 렌더 실측이 정확 재현

```
1c   shell 11 · props 5 · formfactor 1 · residual_resolved 1  = 18  ✔
     formfactor = `‹` 1건뿐
     props = RFQ / 방금 재고관리에서 생성됨 / BCP × 9개 / 재고관리에서 연동 / 현재 1 · 안전 10
            → 뷰포트가 아니라 진입 컨텍스트임이 실측으로 확정
1b   shell 11 · props 7 · formfactor 2 · residual 3 = 23  (fixture 24 − labels[0])
```

### 슬롯 결정 반영

```
포함    소싱에서 찾기   → 1c.vendor.find_sourcing  (렌더 실측 `🔍 소싱에서 찾기` 존재)
제외    수량 변경       → 렌더 실측 이 시안 1c 에 **애초에 없다**(소싱 1b 에만). forbidden 등재
미채택  "공급사 지정 시 발송 검토 모달로 이어집니다" → 렌더 실측 이 시안에 없음
```

---

## 9. 🛑 판정 필요 3건 — 구현 착수 전 확정 (fixture 에 `_충돌` 로 등재)

### ① md_stale_em_dash — 규칙 충돌

```
정본 08-01 md line 30/31 은 아직  `발송 검토로 — 공급사 지정 필요` / `나중에 하기 — 발송 대기로 저장`
시안(30b5daae) 은            `발송 검토로 · 공급사 지정 필요` / `나중에 하기 · 발송 대기로 저장`
```

**충돌 규칙("md 가 이긴다")을 기계적으로 적용하면 §1 이 승인한 em dash 수정이 되돌아간다.**
fixture 는 시안(·)을 채택하고 예외 근거를 §1 로 명시했다.
실측: UI 축 U+2014 = 0 · U+00B7 = 14. em dash 5건은 전량 doc_label 4 + annotation 1.

→ **정본 md 를 08-15 전이로 갱신하지 않으면 다음 트랙이 같은 충돌을 다시 만난다.**
(2026-08-16 CLAUDE.md 전역 조항 승격 완료 — 조항은 섰으나 정본 md 본문은 미갱신)

### ② shell 11 은 "슬롯 수" 동일이지 "문자열" 동일이 아니다

두 시안 shell 11 중 **4 슬롯의 라벨이 다르다**:

```
vendor.panel_title    공급사 지정        ↔  받을 공급사를 지정하세요
vendor.add_email      이메일로 추가      ↔  ✉ 이메일로 추가
vendor.rec_name/meta  "Sigma-Tech Korea · 시약 카테고리 거래 4회" + "이전 거래"
                   ↔  "Sigma-Tech Korea" + "이전 거래 2회 · PBS 계열"   (2노드 분할 재배치)
```

다중집합 대조는 **문자열 축**이므로 공통 컴포넌트 구현 시 슬롯 라벨을 1개로 확정해야 한다.
또한 **정본 기준 shell = 12 텍스트 슬롯 (+ 검색 input attr 1 = 13)**.
11 은 두 시안의 **교집합값이지 정본값이 아니다** — §4 표의 11 을 그대로 구현 목표로 쓰면 정본이 깎인다.

### ③ 아이콘 접두 규정 공백

md 는 `소싱에서 찾기` / `이메일로 추가` / `공급사 소싱에서 먼저 찾기` 를 백틱 라벨로만 적고
아이콘(🔍 ✉) 유무를 규정하지 않았다. **"md 가 이긴다" 를 적용할 근거 자체가 없다.**
fixture 는 렌더 실측을 `label`, md 문자열을 `spec_label` 로 병기했다.

---

## 10. 부수 실측 (fixture 기록 · 이 트랙에서 처리 안 함)

```
RFQ mono weight   1c.header.rfq 600 ✓ / 1d.card.rfq 400 ✗ → md 타이포 토큰 위반. md 승 → 1d 도 500/600
tabular-nums      phone frame 이하 100/100 준수 (전역)
amber/yellow 혼재  §5 실측 재확인 — ②-b + §0-C 별도 배치. §6 대로 fixture 를 붙잡지 않음
colors 축         fixture 자기 무결성 전용. 도출축 = phone frame 이하 inline style 선언 색 토큰
                  (rgb()→hex, alpha<1 은 @a). 렌더 computed 와 모집단이 달라 상호 대체 불가
radius/font_sizes  전 요소(frame + 하위 전량) 측정 — 텍스트노드 부모만 재면 카드 컨테이너를 놓친다
```

```
확인 필요  소싱 시안에 검색 input(placeholder)이 있는지 미측정. 소싱 fixture 는 텍스트노드 축만 떠서
          attr 라벨 기록이 없다. 정본 md 가 검색 input 을 요구하므로 shell 판정 자체는 안 흔들리나,
          "소싱 시안이 정본을 몇 개 빠뜨렸는가" 의 정확한 수는 미확정 (현재 확인분 = `소싱에서 찾기` 1건)
확인 필요  reorder-handoff-comp.json 에 대응하는 conformance test 는 만들지 않았다.
          자기참조 게이트를 하나 더 늘리는 대신 _reorder_comp_probe.mjs 로 재도출 경로를 남겼다.
          렌더 산출물을 compareLabels() 에 물리는 작업은 이 트랙에서 여전히 미착수
```

---

## §9 규칙 등재 2건 (2026-08-16)

### §scope-from-verdict — 판정과 지시문 범위는 **양방향**으로 역산한다

```
축 1 (전방)  확인 범위 ⊇ 판정 기준.
            범위 < 판정이면 조치자가 **멈춘다** — 범위를 넓히는 것도
            판정을 좁히는 것도 조치자 권한 밖이다.
            실례: 2026-08-16 아이콘 분리
                  (범위 1c 2슬롯 < 판정 "전량 0", 1b.cta.secondary 1건 누락)

축 2 (후방)  판정이 **닫은 것**을 같은 지시문이 지운다.
            판정 확정 시 그 판정이 해소한 미결 항목(_충돌 · TODO · 미결 표기)을
            해소기록으로 이동시킨다. 안 지우면 다음 세션이 미결로 읽고 **재판정한다**.
            실례: 2026-08-16 _충돌.md_시안_라벨차_아이콘접두
                  (판정 6번이 닫았는데 잔존)
```

🛑 **이 조문은 1회차 시점에 축 2 가 없었다** — 규칙이 **자기가 기술하는 결함의 형태**를
   갖고 있었다. 규칙 카드를 쓸 때 **"이 규칙이 만든 결과를 누가 치우는가"** 를 같이 적는다.
   §measurement-layer-blindness 가 **"발동 조건"** 을 빠뜨린 것과 같은 자리다.

### §axis-of-divergence — "갈렸다" 는 **어느 축의 진술인지** 적는다

```
시안 ↔ 시안      두 목업의 문자열이 다르다
fixture 상태     fixture 가 어느 값을 들고 있다
fixture ↔ 시안   fixture 와 렌더가 다르다
```
**서로 다른 축이다.** 축을 안 적으면 옮겨 적는 과정에서 바뀐다.

실례 (2026-08-16 §1): 에이전트 회신의 "shell 4슬롯 라벨이 갈렸다" 는 **시안↔시안** 축이었는데,
지시문에 **fixture 상태** 축으로 옮겨 적혔다. 실제 fixture 변경분은 `add_email` **1건**이고
```shell_label_divergence``` 키는 애초에 없었다.

→ **§measurement-layer-blindness 7회차.** 앞 6회와 달리 **총괄이 냈고**,
  잡은 것은 §2 에서 범위/판정 어긋남을 선언하고 멈춘 조치자였다.

### §check-axis-narrower-than-policy — 검사 축이 정책 축보다 **좁으면** GREEN 인 채로 정책이 깨진다

```
정책  "amber 금지"                    CLAUDE.md §9
검사  "amber **클래스** 금지"          amber-removed sentinel 4건(302d6a3b · 6d-1 · 6d-2 · 6d-3)
간극  hex 78건                        4건이 전부 GREEN 이면서 통과시킨 것
```

sentinel 이 **4건이나** 있어서 "amber 는 이미 잡혀 있다" 가 성립하는 것처럼 보였다.
절반만 참이었다 — 302d6d3 주석이 스스로 적고 있었다:
`chart palette hex(#f59e0b) 는 Tailwind class 가 아니라 sweep 무관(보존)`.
**게이트 수는 커버리지가 아니다.**

→ 규칙 카드를 쓸 때 **"이 검사가 정책의 어느 부분을 덮는가"** 를 같이 적는다.
  덮지 않는 부분을 적는 쪽이 더 중요하다 — 그게 다음 사람이 믿어버리는 자리다.

🛑 **§measurement-layer-blindness 와 다른 얼굴이다.** 축이 **안 적힌** 게 아니라
   **좁게 적혔다.** 안 적힌 축은 "측정 안 했다" 로 읽히지만, 좁게 적힌 축은
   **"측정했다" 로 읽힌다** — 후자가 더 위험하다.

같은 형태 2회차(같은 배치 안에서): 모집단 `라이브 14파일` 도 §0-B 의 8토큰으로
좁혀진 값이었다. amber/orange 전종으로 재계수하니 **24파일 / 81건**.
14 로 잠갔으면 10파일 20건이 게이트 밖에 남는다 — **고치려던 실패 형태의 축소판**이다.

### §decision-retired ≠ 구현 종속 — 은퇴 계약 3분류에 4번째가 있다

dead 표면 단언을 (a)구현 종속 / (b)정책 계약 / (c)중복 보장 으로 가르려 했으나
2026-08-16 실측 26건 중 **8건이 어디에도 안 들어갔다.**

```
(d) 결정 은퇴   승인된 결정이 그 계약을 **뒤집었다.** 잃어버린 게 아니다.
               실례: v21 §1(2026-08-09 승인) — 완성도 게이지 buyer 은퇴,
                     체크리스트 액션 전면 폐기(buyer 액션 0)
```

(a) 로 묶으면 "구현이 바뀌었다" 로 읽혀 **결정이 있었다는 사실이 사라진다.**
(b) 로 묶으면 재조준 대상을 찾다가 없어서 "계약이 깨졌다" 로 오판한다.

🛑 **(d) 중 일부는 정책이 살아남는다 — 라벨만 죽는다.**
   `등록이 필요한 정보 ({length})` 는 은퇴했지만 **"미등록을 은폐하지 않는다"** 는 살아 있고,
   그걸 잠그는 단언은 **없었다.** (d) 를 통째 삭제했으면 같이 사라졌다.
   → (d) 는 삭제 전에 **"뒤집힌 계약인가 · 라벨만 바뀐 계약인가"** 를 한 번 더 가른다.
     뒤집힌 것은 **역방향 잠금**이 필요하다(금지의 잠금). 은퇴가 아니다.
