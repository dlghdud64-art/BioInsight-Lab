# §product-detail-refinement §7.6 — fixture 렌더 검증 완료

작성 시각: 2026-08-15
상태: **렌더 대조 실행 완료.** 인계문서 §1 첫 줄 해소.

---

## 0. 결론 먼저

```
fixture 라벨 112     🟢 승격 — 렌더에서 전량 확인 (missing 0)
총 텍스트노드 130    🔴 폐기 — 실측 126
colors 앵커          ⚪ 비교불가 판정 — 렌더로 검증되는 지표가 아님
게이트 3실증          🟢 실측 GREEN 9/9 (이 세션에서 최초 가동)
```

---

## 1. 도출 경로 — 판정자/도출자 분리 위반 해소

인계문서 §8이 지적한 "판정자가 도출을 겸했다"에 대한 조치.

```
시안 파일 이중 확인
  채팅 업로드본   /root/.claude/uploads/…/5cb66efb-….html
  디스크 원본     C:\Users\young\Desktop\소싱 견적 담기 흐름 개선 (단독).html
  sha256          6d98bd270f728714c2055c53beb90f4fd4e72ff65d58fd5e9c897d1e762543f5
  판정            동일 바이트. 두 경로가 같은 산출물을 가리킴
```

렌더 도출은 정적 추출과 **다른 층**에서 수행됐다 — Chromium 헤드리스 실제 렌더,
`TreeWalker(SHOW_TEXT)` + `getComputedStyle`. 정적 추출물을 참조하지 않았다.

```
엔진      Chromium 1194 (Playwright)
viewport  1440 × 2400
대기      "Unpacking" 소멸 대기 + 6s
pageError 0 · console error 0
bodyH     2400 · y max 2273  (콘텐츠 잘림 없음)
```

---

## 2. §4 6개 항목 판정

### ① 총 텍스트노드 130 일치 여부 — 🔴 불일치

```
인계문서 주장(정적)  130 = 라벨 112 + 주석 11 + 셸·헤더 7
렌더 실측            126 = 라벨 112 + 문서메타·주석 14
차이                 4
```

**렌더가 이긴다(§4 규칙). 130 폐기, 126이 정본.**

차이의 주된 몫은 번들러 셸 3건(`Bundled Page` / `This page requires JavaScript to display.` /
`Unpacking...`) — 언팩 완료 시 DOM에서 사라진다. 정적 추출은 언팩 **전** 상태를 세었다.
`document.title` 도 언팩 후 빈 문자열이 된다.

### ② 1a colors 합계 103 — computed 기준 동일한가 — ⚪ 질문 자체가 미성립

```
fixture colors     hex 토큰 출현 횟수 (배경·보더·텍스트 전부)
                   1a 27항목/103 · 1b 23항목/51 · 1c 23항목/57 · 총 73항목/211
렌더 computed      텍스트노드의 color 만 — 고유 15종 / 126건
```

**두 지표는 같은 것을 재고 있지 않다.** 1a 는 라벨 51개인데 colors 합계가 103 이다 —
텍스트 색상 카운트라면 라벨 수를 넘을 수 없다. 세 섹션 모두 1.5~2.1배(§표 참조).

```
1a  라벨 51 · colors 103  → 2.02배
1b  라벨 24 · colors  51  → 2.13배
1c  라벨 37 · colors  57  → 1.54배
```

→ **fixture `colors` 의 성격을 재기술해야 한다.** "화면 색상"이 아니라
**"시안 소스의 hex 토큰 출현 횟수"** 다. 렌더로 검증되지 않으며, 게이트 line 88–92 의
colors 앵커는 시안 정합이 아니라 **fixture 자기 무결성**만 잠근다.

참고 — 렌더 computed color 실측 15종:
```
30  rgb(148,163,184)   20  rgb(15,23,42)      15  rgb(100,116,139)
14  rgb(29,78,216)     11  rgb(21,128,61)      9  rgb(71,85,105)
 7  rgb(255,255,255)    7  rgb(37,99,235)      4  rgb(180,83,9)
 3  rgb(51,65,85)       2  rgb(30,64,175)      1  rgb(203,213,225)
 1  rgb(146,64,14)      1  rgb(67,56,202)      1  rgb(59,130,246)
```

### ③ 특수문자 3종 보존 — 🟢 보존

```
U+2715 ✕    렌더 1 · fixture 1
U+2212 −    렌더 1 · fixture 1
U+FF0B ＋   렌더 1 · fixture 1
ASCII 오염   "-" 0 · "+" 0 · "x"/"×" 0
```

### ④ 소스에 있고 화면에 없는 것 (조건부 렌더) — 🟢 0건

fixture 라벨 112 전량이 렌더 텍스트노드에 존재. **다중집합 대조 missing 0.**
중복 레이블(`상세 ›`×3, `PBS-3`×2, `상세 보기`×2, `Cat.`×2) 포함 개수까지 일치.

**이것이 이번 검증의 핵심 산출이다 — fixture 라벨 축은 렌더로 확인됐다.**

### ⑤ 소스에 없고 화면에만 있는 것 (JS 생성) — 🟢 0건

extra 14건은 전량 **시안 문서 자체의 메타·주석**이며 화면 UI 텍스트가 아니다.

```
문서 제목    소싱 제품 상세 · 견적 진행 · 재발주 합류 흐름
문서 부제    1a 제품 상세(담김 상태) · 1b 견적 진행 → 발송 준비 딥링크 · 1c 재발주 → 소싱 컨텍스트 진입
섹션 번호    "1" "1a" "1b" "1b" "1c"      (5건 — "1b" 는 주석 본문 인라인 split 포함)
주석 라벨    "수정:" "동작:" "흐름:"
주석 본문    4건 (① 본문이 인라인 요소로 3조각 split)
```

**JS 가 생성한 UI 텍스트는 0건.** 조건부 렌더도 0건이므로,
정적 추출과 렌더 사이에 **UI 층 불일치는 없다.**

### ⑥ radius / font-size 선언값 vs computed — ⚪ 대조 앵커 부재

fixture 는 colors 만 잠그고 radius·font-size 앵커가 없다. 렌더 실측만 기록한다.

```
border-radius   0px 89 · 99px 14 · 12px 7 · 9px 5 · 8px 4 · 7px 3 · 10px 2 · 11px 2
font-size       10.5px 10 · 11px 16 · 11.5px 26 · 12px 27 · 12.5px 15 · 13px 8
                13.3333px 2 · 13.5px 6 · 14px 9 · 14.5px 1 · 15.5px 2
                16px 1 · 17px 1 · 21px 1 · 24px 1
```

`13.3333px` 2건은 rem 환산 반올림이다 — 선언값 대조를 도입하면 정확 일치로 잠그면 안 된다.

---

## 3. 게이트 실측 — 이 세션에서 최초 가동

인계문서는 게이트를 "배선됨 / 미가동"으로 남겼다. **실제로 돌렸다.**

```
vitest 2.1.9 · 격리 환경 (공유 node_modules 미접촉)
Test Files  1 passed (1)
Tests       9 passed (9)      311ms

① corrupt→RED  2건  ✓  중복 1개 누락도 검출 (Set 대조였다면 통과할 형태)
② 오탐 0        1건  ✓  1a·1b·1c 순서 무관 통과
③ 앵커 위장 방어 1건  ✓  부분 로딩 → 총계 불일치로 RED
fixture 자기무결성 5건 ✓  112 / 109 / colors 27·103,23·51,23·57 / 특수문자 3종
```

### 🛑 GREEN 의 한계 — 게이트는 아직 시안을 보지 않는다

테스트 파일 12–15행이 스스로 적어놓은 그대로다. `compareLabels()` 가 화면 산출물에
물려 있지 않다. ①②③ 실증은 전부 **fixture 를 fixture 와** 비교한다
(`compareLabels(exp, [...exp].reverse())` — 자기 자신의 역순).

**게이트를 안 돌려서 몰랐던 게 아니라, 돌려도 시안을 안 보게 배선돼 있다.**
이번 렌더 대조는 게이트 **밖에서** 수행됐다. Phase 3 에서 렌더 산출물을
`compareLabels()` 에 물리는 것이 남은 일이다.

---

## 4. fixture 등급 갱신

```
        이전                    이후
내용    유효로 보임              🟢 라벨 112 렌더 확인 (missing 0 · extra UI 0)
도출    정적 추출, 렌더 미확인    🟢 Chromium 실렌더 + computed style
검증    독립 운영자 0            🟡 동일 운영자 · 독립 층(정적↔렌더) 교차
```

**라벨 축은 승격. colors 축은 승격 아님** — 렌더로 검증되는 지표가 아니라는 판정.

### fixture 교체 여부 — 불필요

라벨 112 가 렌더로 확인됐으므로 fixture 라벨은 그대로 유효하다.
§5 의 "교체 시 게이트 3실증 재실행"은 발동하지 않는다. (그럼에도 위 §3 에서 재실행 완료.)

**단, 문서 수치 2건 정정 필요:**
```
총 텍스트노드   130 → 126
colors 앵커     "화면 색상" → "시안 소스 hex 토큰 출현 횟수" (렌더 미검증 지표)
```

---

## 5. 인계문서 §7 배너 건 — 근거 보강

문서 제목이 렌더에서도 확인됐다:

```
소싱 제품 상세 · 견적 진행 · 재발주 합류 흐름
```

부제까지 렌더에 존재한다 — `1c 재발주 → 소싱 컨텍스트 진입`.
**`재발주 합류`는 시안이 스스로 규정한 3대 흐름 중 하나가 맞다.** 렌더로 재확인.

미생성 결정은 유지하되 성격은 §7 기재대로:
```
전  부수 요소. 설계 근거 없음 → 제거
후  시안이 명시한 3대 흐름 중 하나. 불변식 3개 미설계로 이연
```

`§reorder-join-invariant` 카드에 반영 필요.

---

## 6. 다음 세션 첫 줄

**Phase 3 `<CollapsedRow>` 착수 — 렌더 대조 선행 조건 해소됨.**

동시에, 게이트에 렌더를 물리는 작업이 남아 있다:
```
compareLabels(fixture.labels, 렌더_텍스트노드)  ← 아직 배선 안 됨
```
이게 배선되기 전까지 게이트 GREEN 은 **fixture 자기 무결성**만 의미한다.

---

## 7. 이 세션이 남기는 한 문장

> **게이트를 돌리는 것과 게이트가 무언가를 보는 것은 다른 사건이다** —
> 9/9 GREEN 을 실측한 같은 세션에서, 그 GREEN 이 시안을 한 번도 보지 않았다는 것도 실측했다.

---

# 추가 — 설계 핸드오프 md 발견 (같은 세션, 후속)

`C:\Users\young\Desktop\소싱 견적 담기 흐름 핸드오프.md` (5,205 B · 2026-08-09)

시안 HTML 과 **같은 날 같은 위치**에 있던 구현 지시문이다. 문서 §5 는 이 시안을
"시각 truth" 로 명시하고 있다 — 즉 fixture 가 대조해야 할 **스펙 정본**이다.

## A. 🔴 §7 배너 건 — 미생성 근거가 무너진다

인계문서 §7 은 합류 버튼 미생성의 실질 근거를 이렇게 적었다:

> 불변식 3개(이미 다른 재발주안에 있음 / 이미 견적 진행 중 / 권장 수량 충돌)가
> 여전히 미설계이고, 그게 미생성의 실질 근거다.

**핸드오프 md §3 에 설계되어 있다.**

```
line 40  양방향 동기화: 견적 요청서 생성 시 재발주안 상태 → `견적 진행 중`
         (이중 재발주안/이중 견적 생성 방지)          ← "이미 견적 진행 중" 불변식
line 38  견적함 자동 담김: 권장 수량(재고관리 산출) + `재발주 연동` 배지
         + 수량 스테퍼에 "재고관리 권장" 캡션          ← "권장 수량" 처리
line 37  컨텍스트 배너(블루) 문구·복귀 링크 전량 확정   ← 배너 자체가 스펙에 있음
line 47  reorder 파라미터 유실 금지 — 견적 생성까지 전달되어 RFQ↔재발주안 링크 저장
```

3개 중 최소 2개가 설계되어 있고, 배너는 문구까지 확정돼 있다.
미설계로 남은 것은 **"이미 다른 재발주안에 있음"** 1건뿐이다.

### 같은 실패가 두 번째다

인계문서 §7 이 이미 기록했다 — 1차 추출이 헤더를 잘라내서 문서 제목을 못 봤고,
그 상태에서 "설계 근거 없음"으로 판단했다. **이번에는 같은 폴더의 지시문 md 를
안 읽고 "불변식 미설계"로 판단했다.** 판단의 층은 바뀌었지만 형태는 같다.

```
근거 없음  ≠  근거를 안 봄
```

**조치:** `§reorder-join-invariant` 카드는 "불변식 3개 미설계로 이연" 이 아니라
**"불변식 1개(중복 재발주안 감지) 미설계로 이연 · 나머지는 핸드오프 md §3 에 확정"**
으로 재기술해야 한다. 미생성 결정 자체의 재검토가 필요하다.

## B. 🔴 amber 토큰 — 시안이 제품 제약과 충돌

CLAUDE.md 색상 규칙(§11.283a/302c/302d):
```
❌ Tailwind amber-* / orange-* 금지 (16 amber-removed sentinel)
   주의색 = yellow 신호등 · 위험 = red · 정상 = emerald
```

**렌더 실측 — 시안은 amber 를 쓰고 있다:**
```
#b45309 (amber-700)  4건   #92400e (amber-800)  1건
#fde68a (amber-200)  2건   #fffbeb (amber-50)   1건
```

사용처 3곳:
```
1  "2개 보유 · 안전재고 미달"        color #b45309 + border #b45309
2  "이 제품이 담긴 견적을 작성 중입니다"  bg #fffbeb / border #fde68a / text #92400e
   ("작성 중인 견적 열기" 버튼 포함)
3  "이 품목에 등록된 공급사가 없습니다"   color #b45309 + border #b45309
```

### 1번은 단순 amber 금지 위반이 아니다

CLAUDE.md §11.283a 는 **안전재고 미달 = red** 로 잠겨 있다(15+ sentinel).
시안은 이걸 amber 로 그렸다. 구현하면 sentinel 이 RED 를 낸다.

### 2번은 핸드오프 md 의 자기모순

md §1 line 20 은 이 카드를 **"yellow 카드"** 라고 부른다.
md §5 토큰은 `#fffbeb/#fde68a/#92400e` — **amber 다.**
라벨(yellow)과 토큰(amber)이 같은 문서 안에서 다르다.

**조치:** 시안 불일치 항목으로 등재. Phase 3 착수 전 총괄 판정 필요 —
① 시안대로 amber 구현(제약·sentinel 개정 필요) ② yellow 신호등으로 치환
③ 안전재고 미달만 red 로 올리고 나머지 yellow. **판정 전 구현 금지.**

## C. 🟢 radius 14–16 — 준수 확인

md §5 `radius 카드 14–16`. 텍스트노드 부모만 봤을 때는 최대 12px 라 미준수로
보였으나, **전 요소(245개) 측정에서 확인**된다:
```
14px 6건 · 16px 5건        ← 스펙 준수
99px 14 · 11px 10 · 12px 8 · 10px 8 · 9px 5 · 8px 4 · 7px 3
18px 3 · 13px 3            ← 스펙 밖 (pill·소형 요소로 추정)
```
텍스트노드 부모만 재면 카드 컨테이너를 놓친다 — 측정 축 기록용.

## D. fixture colors 축 — 최종 판정

전 요소 computed hex 토큰(색·배경·보더)을 fixture 와 같은 축으로 재측정:
```
렌더    고유 33 · 합계 367
fixture 고유 73 · 합계 211  (1a 27/103 · 1b 23/51 · 1c 23/57)
```
여전히 불일치. 원인은 축이 아니라 **모집단**이다 — 렌더는 색 미지정 요소가
`#000000` 기본값으로 119건 잡히고(소스에 없음), fixture 는 소스 문자열에 실제로
쓰인 hex 만 센다. **양방향으로 서로를 대체하지 못한다.**

```
판정  colors 앵커는 fixture 자기 무결성 지표로만 유효.
      렌더 대조에 쓸 수 없다. 게이트 line 88–92 의 성격을 이렇게 기재할 것.
```

참고 — 인계문서가 "주석 박스 점선 보더로 판단하여 제외"한 `#86efac` 는
렌더에서 3건 존재한다. 제외 판단의 재확인이 필요하다(분류 판단, 실측 아님).

## E. 갱신된 다음 세션 첫 줄

```
1  §reorder-join-invariant 카드 재기술 — 불변식 3개 → 1개
   (미생성 결정 자체 재검토)
2  amber 3곳 총괄 판정 — 시안 vs §11.283a red 잠금 충돌
3  그 다음에 Phase 3 <CollapsedRow>
```

**②의 판정 없이 Phase 3 에 들어가면 sentinel 이 나중에 RED 를 낸다.**
