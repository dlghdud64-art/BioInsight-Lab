# Implementation Plan: §11.352 — 구매 운영 재정의 (발주 인계 게이트)

- **Status:** ✅ 방안 1(구매 운영 surface 한정) 구현·검증 완료 — 푸시 대기. "외부 발주됨 마킹"은 §11.353로 분리.
- **Priority:** 백로그 상위 (라이브 화면 dead-end). DECISION_11.35x 따름.
- **유형:** 워크플로우 재해석 (요청자 중심) · same-canvas
- **Last Updated:** 2026-06-02

> 상위 결정: DECISION_11.35x (요청자 중심 B). LabAxis = 발주 의뢰·추적, 실행은 외부(ERP).

## Phase 0 — 현 구조 (코드 확정)
- 화면 = `/dashboard/purchases/page.tsx` ("구매 운영").
- 큐 = 견적 비교 완료(ready_for_po 이상)분이 "발주 전환 대기"로 진입.
- CTA "발주 전환"(행 980/1488) + "일괄 발주 전환"(헤더 570) → `POST /api/work-queue/purchase-conversion/bulk-po` → **내부 Order(PO) 레코드 생성** → 큐가 "발주 확정" 버킷으로 이동.
- KPI: 발주 전환 대기 / 발주 승인 대기 / 발주 확정 / 공급사 통보 완료.
- **dead-end 본질:** 시스템이 PO를 "발행"하지만 실제 구매는 외부(ERP)에서 일어남 → 내부 PO는 반쪽(외부 발주 여부·입고가 시스템에 없음). 우측 레일도 견적 상세로만 회귀.

## 재해석 방향 (DECISION 따름)
"구매자가 발주 실행" ❌ → "요청자가 **발주 요청을 패키지로 묶어 인계** + **외부 발주됨 수동 마킹** + 인계 후 상태 추적" ✅.

## ⚠️ 승인 필요 — 접근 범위 (구현 전 결정)
재해석이라 범위가 갈립니다. 후보:

- **방안 1 (minimal, 권장):** 라벨/의미만 재정의 + 외부 발주 마킹 추가.
  - "발주 전환" → "**발주 인계**"(또는 "발주 요청 정리")로 라벨/카피 재정의(시스템 PO 실행이 아니라 인계임을 명확히).
  - 기존 bulk-po Order 생성 = "인계 레코드"로 의미 유지(내부 truth) + 인계 후 **"외부 발주됨" 수동 마킹** 액션 추가(§11.353 발주 관리와 연결).
  - 우측 레일 dead-end: 인계 패키지 요약 + 외부 공유(복사/내보내기) 진입점.
  - 장점: 최소 diff, 실 mutation 보존, 라이브 dead-end 즉시 완화.

- **방안 2 (중간):** 방안 1 + **인계 산출물 구조화 export**(ERP/전자결재가 받을 형태, DECISION §4 미래 다리). bulk-po 결과를 export 가능 패키지로.

- **방안 3 (대형):** PO 생명주기 전면 재설계(외부 상태 머신) — §11.353과 통합. 비권장(범위 큼, §11.353 영역).

## 제안 Phase (방안 승인 후)
- Phase 1: 라벨/카피 재정의 contract + "외부 발주됨" 마킹 상태 테스트(실 mutation 보존 회귀).
- Phase 2: CTA/헤더/KPI 카피 재정의 + 외부 발주 마킹 액션 wiring(canonical 상태) + 레일 인계 요약. (+방안2면 export)
- Phase 3: smoke + (vitest sentinel).

## Open Decisions (호영님)
1. 범위 = 방안 1(minimal) / 2(+export) / 3(대형) 중?
2. "발주 전환" 새 라벨 = "발주 인계" vs "발주 요청 정리" vs 기타?
3. "외부 발주됨" 마킹을 §11.352에 포함 vs §11.353(발주 관리)으로 미룰지?

## Must Not
- 실 mutation(bulk-po) wiring 훼손, canonical 상태 손상, dead surface(안 쓰는 구매자용) 정교화, front-only 마킹.
