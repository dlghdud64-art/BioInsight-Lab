# §suite-red-168 — 전체 스위트 상시 RED 168건 (백로그)

**등재 2026-08-17** · §receiving-detail-redesign 배치 중 실측 · 대기열

## 🛑 게이트가 빨간 상태가 상시면 RED 는 신호가 아니다

```
2026-08-17 실측 (프로젝트 vitest · dashboard + regression + design)
  배치 전 (aeb688bc)   44파일 168건 실패
  배치 후 (4b7eb82f)   44파일 168건 실패
  → 이번 배치가 만든 신규 실패 **0건**
```

**측정 방법**: `git checkout HEAD~1` 로 배치 이전 상태를 만들어 같은 명령을 돌렸다.
stash 가 아니라 커밋 대조를 쓴 이유 — 이 배치는 파일 삭제 2건을 포함해 stash 로는 상태가 안 맞는다.

⚠️ 이 카드는 **"168건이 무해하다" 는 뜻이 아니다.** 이번 배치와 무관하다는 것만 증명했다.
168건 각각이 실제 결함인지 sentinel 노후인지는 **미판정**이다.

## 왜 지금 등재하나

RED 168 이 상시면 새 RED 가 묻힌다. 오늘 배치에서 실제로 그 위험이 있었다 —
receiving 관련 RED 6건이 168건 속에 섞여 있었고, **파일명으로 걸러내지 않았으면 못 봤다.**

```
게이트가 잡아야 하는 것   이번 변경이 만든 회귀
게이트가 실제로 내는 것   상시 168 + 이번 변경분
→ 차이를 사람이 매번 손으로 걸러야 한다
```

## 반례 — "무관" 은 배치 기준이지 항상이 아니다 (2026-08-19 추가)

대기열 ⑦ 을 **"오늘 배치들과 무관 확정"** 으로 적었었다. 그 근거는 8/17 배치 기준의 무관이었는데
항목 이름에 "확정" 을 박아 **항상 무관**으로 읽히게 만들었다. 반례가 나왔다:

```
2026-08-19  A 트랙 잔여(보관 위치 정직 표기)로 ReorderReviewSheet.tsx 진입
            그 파일을 읽는 sentinel 이 **이미 RED 4건**이었다 (stash 대조로 기존분 확정)
              inventory-reorder-vendor-history-p4      2건
              inventory-reorder-surface-unify-p3b      2건
            기대 `params.set("supplier"`  vs  소스 `prefill: "reorder-recommendation"` (L205)
            성격: sentinel↔소스 드리프트
```

🛑 168 은 "나중에 볼 백로그" 가 아니라 **작업 경로 위에 있을 수 있다.**
   경로에 있으면 매 배치마다 사람이 손으로 기존분/신규분을 갈라야 하고, 그 판정이 한 번 틀리면
   신규 회귀가 168 에 묻힌다.

⚠️ 1회차다. 이것만으로 우선순위를 올리지 않는다 — 표기만 정정한다.
   **다음에 이 카드를 열면 먼저 잴 것**: 드리프트가 몇 파일에 걸쳐 있나.
   파일 단위로 갈리면 경로 위 노출 빈도가 나오고, 그때 ⑦ 의 우선순위가 근거를 갖는다.
   (지금 올리면 1건으로 정하는 것이라 §7 "표기를 근거로 삼지 않는다" 를 그대로 반복한다.)

## 📐 범위 정정 (2026-08-19 · ⑦-확대 0단계)

**168 은 좁은 스코프의 값이었다.** 전체 스위트 실측:

```
              카드 원값(2026-08-17)      전수 실측(2026-08-19 · HEAD 837c48a6)
스코프         dashboard+regression+design   전체 (npx vitest run)
실패           168                           **242**
실패 파일       44                            **67**
전체 it        (미측정)                       12,126 (passed 11,779)
실행 파일       (미측정)                       1,219
```

🔑 **`project_full_suite_triage` 메모리(242/66)와 같은 모집단이다.**
   카드 §범위가 "대조 안 됨" 으로 열어둔 항목이 이것으로 닫힌다.
   두 기록이 다른 숫자를 든 이유는 결함이 아니라 **스코프가 달랐기 때문**이다.

🛑 **파일 이름·§번호의 `168` 은 그대로 둔다.** 식별자이지 측정값이 아니다.
   바꾸면 대기열 ⑦ 참조와 이 카드를 인용한 커밋들이 끊긴다.
   (CLAUDE.md §11.302c 가 "이제 색상이 아니라 dead-file 차단으로 재정의" 된 것과 같은 취급.)

### 분포 — 축에 따라 결론이 뒤집힌다

```
테스트 파일 축   상위 5 = 31% · 상위 10 = 48% · 67파일     → 분산
겨냥 소스 축     상위 3 = 63% · 상위 5 = 76% · 32종        → 집중
```

```
  90  (19파일)  38%   src/app/dashboard/quotes/page.tsx
  41  ( 9파일)  55%   src/app/_workbench/search/page.tsx
  20  ( 4파일)  63%   src/app/dashboard/inventory/inventory-content.tsx
  18  ( 1파일)  71%   src/app/api/compare-sessions/[id]/insight/route.ts
  11  ( 1파일)  76%   apps/mobile/app/compare/[id].tsx
```

→ **파일 단위가 아니라 겨냥 소스 단위로 접근한다.** 소스 3개가 151건을 덮는다.
  `242 × git log -S` 는 필요 없다.

### ⚠️ 계측 한계 — 이 숫자를 정밀값으로 읽지 말 것

```
① 귀속 휴리스틱   테스트 파일 1개 = 겨냥 소스 1개 로 잡았다(가장 많이 언급된 것).
                  중복 계수를 없애려는 선택이고, 여러 소스를 실제로 겨냥하는 파일은
                  그중 하나에만 계상된다. 소스별 건수는 **하한**이다.
② 미귀속 4건      2개 파일(sds-document-model-348b1 · csrf-batch10)에서
                  경로 표기를 못 읽었다. 242 중 238 만 귀속됐다.
③ 경로 정규화 결함 `src//dashboard/audit/page.tsx` (슬래시 2개)는 실제 경로가 아니라
                  내 정규화 버그다. 7건이 그 키에 붙어 있다.
```

🛑 ①~③ 을 지우고 표만 인용하면 이 카드가 후보 ④(요약이 단서를 떨어뜨림) 사례가 된다.

## 📋 배치 조건 문구 정정 (2026-08-19)

```
전  "사유가 다르고 섞으면 배치의 원인 단일성이 깨진다"
후  "**원인**이 다른 것을 섞지 말라"
```

원인과 처분을 구분하지 않아 생긴 부정확이다. §11.279 배치가 실례다 —
**원인 1개(fd86d1c4) · 처분 3개((가)은퇴 ·(나)구멍 메움 ·(다)재조준)** 이고 조건 위반이 아니다.

🛑 처분이 갈리는 것은 정상이다. 오히려 **처분이 하나로 보이면 대조를 안 한 것**을 의심한다 —
   §11.279 도 대조 전에는 "60건 은퇴" 한 갈래로 보였다.

## 범위 — 미측정

44파일의 성격을 안 갈랐다. 알려진 것만:

```
관련 백로그   project_full_suite_triage (메모리) — 실측 assertion 242/66 + collection 41 = 107파일
              "확정 B 0 · ai-pipeline 38건 test-resolution 전용" 이라고 적혀 있다
             → 이 168 과 같은 모집단인지 **대조 안 됨**
파일 계열     대부분 dashboard/quotes · regression. receiving 계열은 이번 배치로 0
```

## 착수 시 선행 판정

```
1  이 168 이 project_full_suite_triage 의 107파일과 같은 모집단인가
   같으면 그 카드에 합류 · 다르면 별건
2  분류 축 — 오늘 세운 3분류가 그대로 쓰인다
   (a) sentinel 낡음   구현이 진화, 검사가 안 따라옴  → 승계
   (b) 제품 결함       → 미이행 목록
   (c) 중복 보장       → 삭제
   + (d) 결정 은퇴     승인된 결정이 계약을 뒤집음   → 이력 보존 + 역방향 잠금 검토
3  🛑 삭제로 끝내지 말 것 — 오늘 두 번 겪었다. 근거(sha·사유)를 강제해야 재발이 막힌다
```

## 관련

- 측정 커밋 `4b7eb82f` (§receiving-detail-redesign P1~P3)
- 3분류 + (d) 근거: `DECISION_reorder-handoff-2026-08-15.md` §9 `§decision-retired`
- 오늘 승계 실례: `87642da5`(reorder-review-310 4건) · 이번 배치(quarantine 3건)
