# 판정 — §reorder-join-invariant 근거 교체 · amber 3곳 분해 배치

작성 시각: 2026-08-15
판정자: 호영님(총괄) · 도출: 렌더 실측 + 대비 계산
선행 커밋: `500878cb` (렌더 검증 결과)

---

## 0. 확정 사항 2건

```
①  배너 미생성 — 유지. 단 근거 문장 교체 (§1)
②  amber 3곳 — 분해 배치. a=Phase 3 동반 · b=별도 (§2)
    §0-B amber 8토큰 전수 일치 게이트 — 보류. 색 무관 GREEN 진행 (§3)
```

---

## 1. §reorder-join-invariant 카드 — 교체 문안

**아래를 카드에 그대로 반영한다.**

```
전  부수 요소. 설계 근거 없음 → 제거
전' 시안이 명시한 3대 흐름 중 하나. 불변식 3개 미설계로 이연

후  시안이 명시한 3대 흐름 중 하나. 불변식 1개 미설계로 이연.

    미설계 1건 — "이미 다른 재발주안에 있음"
    설계 완료 3건 — 핸드오프 md §3
      line 37  컨텍스트 배너 문구·복귀 링크 확정
      line 38  권장 수량 자동 담김 + `재발주 연동` 배지
      line 40  양방향 동기화 → 재발주안 `견적 진행 중`, 이중 생성 방지

    🛑 3/4 설계됨을 근거로 미생성 결정을 뒤집지 말 것.
       line 40 은 방어 **대상**을 정의했을 뿐 방어 **판정 기준**이 없다.
       무엇을 막을지는 적혀 있고 무엇이 중복인지가 없다.
       이 상태로 쓰기를 열면 방어 문구가 있어서 안전해 보이는 경로가 된다
       (deleteMany 트랜잭션화 때와 동형).

    착수 조건 — "이미 다른 재발주안에 있음" 판정 기준 확정.
```

### 왜 문장 교체가 필수인가

"불변식 3개 미설계"는 이제 **사실이 아니다.** 이 문장을 두면 다음 세션이
핸드오프 md §3 을 읽고 "이미 다 설계됐다"로 뒤집는다. 근거가 틀린 채로 결론만
맞는 카드는 결론을 지키지 못한다.

### 이 건의 성격 — 동형 반복 2회

```
1회  1차 추출이 헤더를 잘라내 문서 제목을 못 봄 → "설계 근거 없음" 판단
2회  같은 폴더의 지시문 md 를 안 읽음        → "불변식 3개 미설계" 판단
```

두 번 다 **근거 없음 ≠ 근거를 안 봄**이었다. 판정 전 소스 전수 확인을 절차에
넣지 않으면 3회가 난다.

---

## 2. amber 3곳 — 분해 배치 (확정)

권고 원안은 세 곳을 §0-B/§0-C 재판정 배치로 통합했으나, **성격이 달라 분해한다.**

### ②-a — Phase 3 동반

```
대상    "2개 보유 · 안전재고 미달"      #b45309 (color + border)
충돌    §11.283a 안전재고 미달 = red   ← 신호등 **의미** 충돌
조치    #b45309 → red 토큰
근거    red 는 §0-B 에 이미 정본으로 존재. 토큰 정본 변경 아님.
        주의색을 위험색으로 올리는 것이므로 §0-B 재판정 불필요.
대비    #b91c1c on #ffffff = 6.47 (AA ✓) · on #fef2f2 = 5.91 (AA ✓)
```

**§0-B 를 건드리지 않으므로 Phase 3 배치에 포함한다.**

### ②-b — 별도 배치

```
대상    "이 제품이 담긴 견적을 작성 중입니다" 카드
          bg #fffbeb · border #fde68a · title #92400e · body/CTA #b45309
        "이 품목에 등록된 공급사가 없습니다"   #b45309
충돌    amber-* 금지 (16 amber-removed sentinel)
조치    amber → yellow 신호등 치환
전제    §0-B amber 8토큰 재판정 · §0-C(amber 채택 근거) 재판정
```

**별도 배치의 근거는 "규모"가 아니라 "층"이다.**
§0-B 는 토큰 정본, Phase 3 는 컴포넌트다. 규모로 미루면 다음에 규모가
작아 보일 때 섞인다. 층이 다르다는 근거는 규모와 무관하게 유지된다.

### §0-C 재판정 입력값 — 대비 실측

**yellow 치환은 AA 를 잃지 않는다.** §0-C 가 대비 확보를 amber 채택 근거로
들었다면 그 근거는 성립하지 않는다.

```
현행 amber                          치환 yellow
#b45309 / #fffbeb   4.84  AA ✓      #a16207 / #fefce8   4.76  AA ✓
#92400e / #fffbeb   6.84  AA ✓      #854d0e / #fefce8   6.62  AA ✓
#b45309 / #ffffff   5.02  AA ✓      #a16207 / #ffffff   4.92  AA ✓
#b45309 / #faf9f5   4.77  AA ✓      (페이지 bg)
```

§0-B 의 `4.84` 는 **amber-700 on amber-50** 임이 확인됐다(실측 일치).
최대 손실은 0.22 (6.84 → 6.62)이며 전 항목 AA 유지.

계산식: WCAG 2.1 상대휘도 — `L = 0.2126R + 0.7152G + 0.0722B`
(sRGB 선형화 후), `CR = (L₁+0.05)/(L₂+0.05)`.

### 🟡 별건 등재 — 보더 대비 미달 (치환과 무관)

```
#fde68a on #fffbeb  = 1.20   ✗ 비텍스트 3.0 미달  (현행 amber)
#fef08a on #fefce8  = 1.13   ✗ 비텍스트 3.0 미달  (치환 yellow)
```

**치환이 만드는 리스크가 아니다 — 현행 시안이 이미 미달이다.**
치환해도 같은 수준이므로 ②-b 판정에는 영향 없다. 카드 보더의 시각적 구분을
대비로 확보하려면 별도 항목으로 다뤄야 한다(양쪽 다 -200 은 부족).

---

## 3. §0-B amber 8토큰 전수 일치 게이트 — 보류 (확정)

```
상태    보류 (Phase 3 기간)
사유    게이트를 살려두면 amber 토큰이 정본과 어긋난 상태에서 RED 가 난다.
        색 판정이 끝나기 전에는 이 RED 가 신호가 아니라 잡음이다.
효과    색을 건드리지 않는 GREEN 1~8 나머지 항목은 Phase 3 에서 진행 가능.
해제    ②-b 배치 완료 시 — §0-B 를 yellow 로 확정한 뒤 새 토큰 기준으로 재잠금.
```

### 🔁 2026-08-16 갱신 — 이 보류의 전제가 틀렸다

**앵커가 dead 였다.** `product-completeness.tsx` 는 importer 0 — 이 게이트는
보류 여부와 무관하게 **아무것도 잠그고 있지 않았다.** 8토큰 계약을 질 수 있는
라이브 표면은 **0건**(라이브 최다 4종)이라 302c 식 재앵커도 불가능했다.

```
① §0-B 파일 계약        은퇴 (앵커 소멸 + 승계 대상 0)
② 방어 범위 승계        §amber-token-ratchet — 파일 축 → **토큰 축** 전역
                        baseline 라이브 24파일 / 81건. 증가 RED · 감소도 RED
③ 정책                  불변 — amber 금지 · yellow=주의 · 위험=red · 정상=emerald
```

🛑 **부수 발견 — 기존 amber-removed sentinel 4건이 hex 를 안 보고 있었다.**
   정책은 "amber 금지" 인데 검사는 "amber **클래스** 금지" 였다(302d6d3 주석이
   `chart palette hex 는 sweep 무관(보존)` 이라 스스로 적고 있다). hex **78건**이
   4건 전부 GREEN 인 채로 통과했다. → §check-axis-narrower-than-policy 등재.

```
해제 조건 (구)  §0-B yellow 확정
해제 조건 (신)  ① 토큰 축 ratchet 배선 완료          ✅ f4dc3c61 (2026-08-16)
                ② (a)(c) 삭제 + 도달성 가드 해제      ⏸ 진행 중 — 아래 잔여 참조
                ③ §0-B yellow 확정                    ⏸ 미착수
```

**②의 잔여**: (a)7·(c)6 삭제 완료(a880e850 계열). 그러나 도달성 가드(importer ≥ 1)는
아직 RED 2 — `product-detail-refinement` · `product-detail-completeness-pd-b` 의
**모듈 스코프 `const COMP = root(dead)` 선언**이 남아서다. (b)5·(d)8 단언이 그 선언을
계속 쓴다. **(d)를 보존하라는 결정과 가드를 켜라는 결정이 구조적으로 충돌한다** —
보존하면 참조가 남고, 참조가 남으면 가드가 RED 다. 해소안은 §0-B-succession 회신 참조.

### 🛑 보류에 붙는 조건

**보류는 무기한이 아니다.** 해제 조건(§0-B yellow 확정)을 카드에 같이 적지
않으면 보류가 폐지로 읽힌다. 인계문서 §11 이 기록한 형태 — "진행 중"이
"완료됨"으로 읽히는 것 — 의 색상판이다.

```
게이트 상태 표기 필수:  🟡 보류 (해제 조건: §0-B yellow 확정)
                        ❌ "미적용" · "제외" 로 적지 말 것
```

---

## 4. Phase 3 착수 조건 — 갱신

```
유지  기존 착수 조건 전부
추가  ②-a 반영 — "안전재고 미달" red 치환이 Phase 3 배치에 포함
보류  §0-B amber 8토큰 전수 일치 게이트
차단  없음 — 색 무관 GREEN 항목 진행 가능
```

### 렌더 대조 활성 시점

`compareLabels()` 는 아직 화면 산출물에 미배선이다. **Phase 3 EXIT 이 첫 시점**이며,
그때까지 게이트 GREEN 은 fixture 자기 무결성만 의미한다(§7.6 등급 한계 그대로).

---

## 5. 미이월 — 별도 트랙

```
감사 무결성 트랙   커밋 2 미착수. 감사 실패는 여전히 조용하다.
개발 DB 비밀번호   회전 미완 (비차단, 위생)
보더 대비 미달     §2 별건 등재분 — 담당 배치 미정
```

---

## 6. 이 판정이 남기는 한 문장

> **근거가 틀린 채로 결론만 맞는 카드는 결론을 지키지 못한다** —
> 미생성 결정은 유지됐지만, 그 결정을 살린 것은 결정 자체가 아니라
> "무엇이 중복인지가 없다"는 한 줄이었다.
